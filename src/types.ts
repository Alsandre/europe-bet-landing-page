export type TBoardProps = {
  children: React.ReactNode;
  title: string;
  className?: string;
};
export type TTableItemProps = {
  place: string;
  voucher: string;
  prize: string;
  className?: string;
};
